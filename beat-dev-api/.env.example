# AWS / S3
AWS_REGION=ap-northeast-2
S3_BUCKET=beat-dev-bucket-name

# (선택) S3 경로 prefix
S3_UPLOAD_PREFIX=uploads
S3_RESULT_PREFIX=results

# (선택) presigned URL 만료(초)
PRESIGN_EXPIRE_SECONDS=3600

# (선택) 내부 콜백 테스트용 (운영에선 보통 미사용)
CALLBACK_TOKEN=change-me
